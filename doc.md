# AI 编程任务指令文档：项目架构与编码风格优化

## 任务概述
请根据本指令文档，**首先**在**当前项目根目录**下生成一份 `工作计划.md` 文件。该计划将用于指导后续的 AI Coding 流程，要求流程清晰，并支持**多 Agent 协同工作**。

核心工作目标：  
参考优秀项目 `C:\Users\13412\code\js\front\vue3-admin`，对**当前目录下的 Vue3 + Vite 前端项目**进行**编码风格、架构、代码结构**等方面的优化改造。

> ⚠️ 重要约束：
> 1. **两个项目本质上不是同类业务项目**，因此改造**不得破坏当前项目的现有功能和外观**。允许对 HTML 结构、组件划分等进行**适当优化**，但核心目标是提升**编码风格、可维护性、可读性与拓展性**。
> 2. 改造应基于前端通用最佳实践（如组件化、组合式 API、关注点分离等），而非简单拷贝另一个项目的业务逻辑。

---

## 一、任务目标（详细说明）

### 1. 参考项目分析（只参考风格与架构）
- 参考项目路径：`C:\Users\13412\code\js\front\vue3-admin`
- 需要参考的方面：
  - 目录结构与模块划分方式
  - 组件命名、文件命名规范
  - 组合式 API（Composition API）的使用模式
  - 状态管理（Pinia/Vuex）的组织方式
  - 静态资源、工具函数、API 请求层的封装结构
  - ESLint / Prettier 代码规范配置
  - Vite 配置优化

- **不需要参考**：业务逻辑、UI 界面、路由结构、具体接口调用。

### 2. 当前项目的改造范围（不破坏功能与外观）
- 允许重构：
  - 组件拆分与复用优化
  - 提取公共逻辑到 composables
  - 统一代码风格（缩进、引号、命名等）
  - 重构过长或过复杂的文件
  - 优化 HTML 模板中的语义化与可读性
  - 优化 CSS 结构（如使用 scoped、CSS 变量、模块化）

- 禁止操作：
  - 修改用户可见的 UI 布局或交互流程（除非明显冗余且不影响外观）
  - 更改业务逻辑结果
  - 删除或重命名关键路由、页面、API 调用逻辑
  - 破坏现有依赖版本导致项目无法运行

---

## 二、工作流程要求（支持多 Agent 协同）

`工作计划.md` 必须采用**分阶段、可并行**的任务分解方式，便于多个 AI Agent 同时执行不同模块。每个任务应包含：

- 任务 ID（如 `TASK-01`）
- 任务名称
- 依赖任务（若无则写“无”）
- 优先级（高 / 中 / 低）
- 涉及的文件或目录范围
- 具体改造内容描述
- 验收标准（如何判断该任务完成且未破坏功能）

### 推荐的任务阶段划分（示例，可调整）

#### 阶段 0：准备工作（单 Agent）
- 分析两个项目的差异
- 备份当前项目关键配置
- 生成当前项目文件结构树

#### 阶段 1：配置文件与工具链（可并行）
- ESLint / Prettier 统一
- Vite 配置优化
- 路径别名统一（如 `@` 指向 `src`）

#### 阶段 2：目录结构重构（可串行或并行）
- 按功能/模块重新组织 `src` 目录
- 分离 `composables`、`utils`、`apis`、`stores`、`components`

#### 阶段 3：代码风格与组件化改造（可并行，按模块分工）
- 页面组件拆分
- 提取公共 UI 组件
- 将 options API 转为组合式 API（如已有）

#### 阶段 4：可维护性提升（可并行）
- 添加关键类型定义（TypeScript 或 JSDoc）
- 统一错误处理与请求封装
- 添加必要的注释与 README

#### 阶段 5：验证与回归（单 Agent 或串行）
- 启动项目，验证所有原有功能正常
- 对比改造前后的 UI 视觉一致性
- 输出改造报告

---

## 三、AI Coding 执行约束

1. **必须先生成 `工作计划.md`**，不直接修改项目代码。
2. 计划生成后，等待人类确认或调整，再按计划分步执行。
3. 每个 Agent 在执行具体任务时，应遵循：
   - 只修改任务指定的文件范围
   - 不引入外部新依赖（除非计划中明确）
   - 每次改动后执行 `npm run dev` 或对应检查命令验证
4. 若发现会破坏现有功能或外观，立即停止并报告。

---

## 四、输出要求

- 最终生成的 `工作计划.md` 必须存放于当前项目根目录。
- 使用清晰的 Markdown 格式，包含表格或列表展示任务分解。
- 必须在文件开头注明：“本计划由 AI 生成，用于多 Agent 协同改造项目编码风格与架构。”

---