# 工作计划.md

> 本计划由 AI 生成，用于多 Agent 协同改造项目编码风格与架构。

---

## 项目分析摘要

### 当前项目（InnerG）

| 维度 | 现状 |
|------|------|
| 框架 | Vue 3.5 + Vite 7 |
| 语言 | 纯 JavaScript（`.js` / `.vue`） |
| UI 库 | Element Plus（全量引入） |
| 状态管理 | Pinia（`stores/admin.js`） |
| 路由 | Vue Router 4 |
| HTTP | Axios（`utils/request.js`） |
| CSS | Sass |
| API 层 | `api/` 按模块拆分（auth / backend / consult / music） |
| 组件 | `components/` 平铺 12 个组件 |
| 页面 | `views/` 平铺 13 个页面 |
| 路径别名 | **无**（全部相对路径） |
| 代码规范 | **无** ESLint / Prettier |
| Element Plus | 全量导入 |
| SCSS 主题 | **无**全局 SCSS 注入 |

### 参考项目（vue3-admin）可借鉴的模式

| 模式 | 描述 |
|------|------|
| 路径别名 `@` → `src` | 统一引用路径，提升可读性 |
| Element Plus 按需自动导入 | `unplugin-vue-components` + `unplugin-element-plus` |
| SCSS 全局注入 | `additionalData` 注入 Element Plus 主题覆盖 |
| `<style scoped>` | 所有组件使用 scoped 样式 |
| `defineExpose` 模式 | 子组件暴露方法供父组件调用 |
| `<script setup>` | 全部使用组合式 API |

---

## 阶段 0：准备工作（串行执行）

| 任务 ID | 任务名称 | 依赖 | 优先级 | 涉及范围 | 改造内容 | 验收标准 |
|---------|----------|------|--------|----------|----------|----------|
| **TASK-00** | 项目差异分析报告 | 无 | 高 | 整个项目 | 对比两个项目的目录结构、配置、编码风格差异，输出差异清单 | 生成一份差异对比表，列出所有可优化项 |
| **TASK-01** | 生成文件结构快照 | 无 | 中 | `src/` 全部 | 运行 `tree` 或手动列出 `src/` 下全部文件及层级，保存为 `树结构快照.md` | 包含 src 下所有文件的完整路径列表 |
| **TASK-02** | 备份关键配置文件 | 无 | 高 | `vite.config.js`、`package.json`、`config/` | 将 `vite.config.js`、`package.json`、`config/config.yaml` 复制到 `备份/` 目录 | 备份文件与原文件内容一致 |

---

## 阶段 1：配置文件与工具链（可并行执行）

| 任务 ID | 任务名称 | 依赖 | 优先级 | 涉及范围 | 改造内容 | 验收标准 |
|---------|----------|------|--------|----------|----------|----------|
| **TASK-10** | 添加 ESLint + Prettier 配置 | TASK-02 | 高 | 根目录配置、`package.json` | 1. 安装 `eslint`、`@eslint/js`、`eslint-plugin-vue`、`prettier`<br>2. 创建 `eslint.config.js`（flat config）<br>3. 创建 `.prettierrc` 配置<br>4. 在 `package.json` 添加 `lint` 和 `format` 脚本<br>5. 添加 `.prettierignore` | `npm run lint` 可执行且仅报告风格问题（非业务错误） |
| **TASK-11** | 添加 Vite 路径别名 | TASK-02 | 高 | `vite.config.js`、**所有** `.vue` / `.js` 文件中的相对路径导入 | 1. 在 `vite.config.js` 的 `resolve.alias` 中添加 `@` → `src`<br>2. 将所有文件中的相对路径导入（如 `../components/`）替换为 `@/` 别名<br>3. 确保 `import.meta` 动态路径不受影响 | 1. `npm run dev` 启动无报错<br>2. 所有页面正常渲染<br>3. 无残留的相对路径引用 |
| **TASK-12** | Element Plus 按需导入 | TASK-02 | 中 | `vite.config.js`、`main.js`、`package.json` | 1. 安装 `unplugin-vue-components`、`unplugin-element-plus`<br>2. 在 `vite.config.js` 中配置自动导入（含 SASS 主题支持）<br>3. 移除 `main.js` 中的 `ElementPlus` 全量注册<br>4. 检查所有使用 ElMessage/ElNotification 等命令式 API 的地方 | 1. `npm run dev` 启动无报错<br>2. Element Plus 组件正常渲染<br>3. 打包体积减少 |
| **TASK-13** | SCSS 全局配置 | TASK-02 | 低 | `vite.config.js`、新建 `src/styles/` 目录 | 1. 创建 `src/styles/element/index.scss` 覆盖 Element Plus 主题变量<br>2. 在 `vite.config.js` 的 `css.preprocessorOptions.scss.additionalData` 中注入<br>3. 创建 `src/styles/variables.scss` 存放全局 SCSS 变量 | 1. `npm run dev` 启动无报错<br>2. Element Plus 组件使用自定义主题色 |

---

## 阶段 2：目录结构优化（依赖阶段 1）

| 任务 ID | 任务名称 | 依赖 | 优先级 | 涉及范围 | 改造内容 | 验收标准 |
|---------|----------|------|--------|----------|----------|----------|
| **TASK-20** | 组件目录分层归类 | TASK-11 | 中 | `src/components/` | 将 12 个平铺组件按类型归入子目录：<br>- `layout/`：AuthLayout、BackendLayout、FrontendLayout、Navbar、Sidebar<br>- `common/`：PageHead、TableSearch、UserInfo、MarkdownRenderer<br>- `business/`：ArticleDialog、RichTextEditor<br>- 保留 `HelloWorld.vue` 在根（示例组件）<br>同步更新所有引用路径 | 1. `npm run dev` 无报错<br>2. 所有布局和组件正常渲染 |
| **TASK-21** | 页面目录分层归类 | TASK-11 | 中 | `src/views/` | 将 13 个平铺页面按功能归入子目录：<br>- `auth/`：Login、LoginEmail、Register<br>- `home/`：Home、Dashboard<br>- `consult/`：Consult、Consultations<br>- `knowledge/`：Know、Knowledge<br>- `mood/`：MoodLog、MoodGarden<br>- `music/`：MusicList、MusicPlayer<br>同步更新 `router/index.js` 中的懒加载路径 | 1. `npm run dev` 无报错<br>2. 所有路由正常访问<br>3. 页面功能不受影响 |
| **TASK-22** | 创建 composables 目录 | TASK-11 | 中 | `src/composables/`（新建） | 从页面/组件中提取可复用逻辑：<br>- `useAuth.js`：登录状态、token 管理<br>- `useRequest.js`：请求加载状态、错误处理<br>- `usePagination.js`：分页逻辑<br>- 创建 `src/composables/README.md` 说明用途 | 1. 提取的 composable 可独立使用<br>2. 原页面功能无退化 |
| **TASK-23** | 统一 request 层封装 | TASK-11 | 高 | `src/utils/request.js` | 参考 vue3-admin 的 axios 封装模式：<br>1. 统一添加 `X-Requested-With` 请求头<br>2. 添加响应拦截器统一解包（`res.data.data`）<br>3. 统一错误码处理与提示<br>4. token 过期自动跳转登录<br>5. 检查所有 `api/` 文件中的调用是否兼容 | 1. 所有 API 调用正常工作<br>2. 异常场景有 ElMessage 提示<br>3. 401/419 自动跳转登录页 |

---

## 阶段 3：代码风格与组件化改造（依赖阶段 1）

| 任务 ID | 任务名称 | 依赖 | 优先级 | 涉及范围 | 改造内容 | 验收标准 |
|---------|----------|------|--------|----------|----------|----------|
| **TASK-30** | 确保全部组件使用 `<style scoped>` | TASK-11 | 高 | `src/components/`、`src/views/` 全部 `.vue` 文件 | 1. 检查所有 `.vue` 文件的 `<style>` 标签<br>2. 对未使用 `scoped` 的添加 `scoped` 属性<br>3. 如有全局样式被移除，迁移到 `src/styles/` | 1. 所有组件样式不互相污染<br>2. 页面外观不变 |
| **TASK-31** | 统一组件代码风格 | TASK-10、TASK-11 | 高 | `src/` 下全部 `.vue` / `.js` 文件 | 1. 运行 ESLint 自动修复<br>2. 手动修复 ESLint 无法自动修复的问题<br>3. 统一：单引号、无分号、2 空格缩进、trailing commas<br>4. 确保全部使用 `<script setup>` 语法（如有 Options API 则转换） | `npm run lint` 通过，无报错 |
| **TASK-32** | 提取公共 UI 组件 | TASK-20 | 中 | `src/components/common/` | 从多个页面中识别并提取可复用的 UI 片段：<br>- 确认对话框组件<br>- 空状态组件<br>- 加载中组件<br>- 搜索表单组件（如通用）| 1. 至少提取 2 个可复用组件<br>2. 替换原页面中的内联代码<br>3. 功能完全一致 |
| **TASK-33** | 优化 HTML 模板语义化 | TASK-11 | 低 | `src/views/`、`src/components/` | 1. 将无语义的 `<div>` 替换为 `<section>`、`<header>`、`<main>`、`<article>` 等<br>2. 添加必要的 `aria-label` 属性<br>3. 确保表单控件有 `<label>` 关联 | 1. 页面渲染不变<br>2. 无 Lighthouse accessibility 倒退 |

---

## 阶段 4：可维护性提升（依赖阶段 2、3）

| 任务 ID | 任务名称 | 依赖 | 优先级 | 涉及范围 | 改造内容 | 验收标准 |
|---------|----------|------|--------|----------|----------|----------|
| **TASK-40** | 添加 JSDoc 类型注释 | TASK-31 | 中 | `src/utils/`、`src/api/`、`src/stores/`、`src/composables/` | 1. 为所有工具函数添加 JSDoc `@param`、`@returns` 注释<br>2. 为 Pinia store 的 state/getters/actions 添加 JSDoc<br>3. 为 composable 的返回值添加 JSDoc | 各函数/方法有清晰的参数和返回值说明 |
| **TASK-41** | 统一错误处理模式 | TASK-23 | 高 | `src/api/`、`src/utils/request.js`、所有调用 API 的组件 | 1. 确保所有 API 调用使用统一的 request 实例<br>2. 检查是否有直接使用 axios 而非 request 实例的调用<br>3. 添加全局 loading 和错误边界处理 | 1. 所有异步请求有统一的错误提示<br>2. 无遗漏的 `.catch()` 空处理 |
| **TASK-42** | SCSS 变量与 CSS 自定义属性 | TASK-13 | 低 | `src/styles/`、全部 `.vue` 文件 | 1. 在 `src/styles/variables.scss` 定义主题色、间距、字体等变量<br>2. 在 `src/styles/global.scss` 定义全局 CSS 自定义属性（`:root`）<br>3. 将组件中硬编码的颜色值替换为变量 | 1. 修改一处变量即可全局生效<br>2. 页面外观不变 |
| **TASK-43** | 更新项目 README | TASK-00 | 低 | `README.md` | 1. 补充项目架构说明<br>2. 添加目录结构图<br>3. 添加开发规范说明（路径别名、组件分层、代码风格）<br>4. 添加可用的 npm scripts 说明 | README 内容清晰完整，新人可按文档启动开发 |

---

## 阶段 5：验证与回归（依赖前面全部阶段）

| 任务 ID | 任务名称 | 依赖 | 优先级 | 涉及范围 | 改造内容 | 验收标准 |
|---------|----------|------|--------|----------|----------|----------|
| **TASK-50** | 全功能回归验证 | 全部前序任务 | 高 | 整个项目 | 1. `npm run dev` 启动项目<br>2. 逐一访问所有路由页面<br>3. 测试核心交互：登录、列表查询、表单提交、路由跳转<br>4. 测试边界状态：网络异常、空数据、token 过期 | 1. 所有页面正常渲染<br>2. 所有原有功能可用<br>3. 控制台无新增报错 |
| **TASK-51** | UI 视觉一致性对比 | 全部前序任务 | 高 | 全部页面 | 1. 对比改造前后各页面的截图<br>2. 检查布局、间距、颜色、字体是否一致<br>3. 检查 Element Plus 组件样式是否正常 | 外观与改造前基本一致，无肉眼可见差异 |
| **TASK-52** | 构建产物验证 | TASK-50 | 高 | `dist/` | 1. 执行 `npm run build`<br>2. 检查构建是否成功<br>3. 对比改造前后的打包体积<br>4. 执行 `npm run preview` 预览构建产物 | 1. 构建成功无报错<br>2. 预览模式下页面正常 |
| **TASK-53** | 输出改造总结报告 | TASK-50、TASK-51、TASK-52 | 中 | 项目根目录 | 生成 `改造报告.md`，包含：<br>1. 改造前后对比<br>2. 文件变更列表<br>3. 遗留问题与改进建议<br>4. 后续优化方向 | 报告内容完整，可作为项目交付文档 |

---

## 执行依赖关系图

```
阶段 0（串行）
  TASK-00 ──┬── TASK-01
            └── TASK-02
                   │
阶段 1（可并行）    │
  TASK-10 ←────────┤
  TASK-11 ←────────┤
  TASK-12 ←────────┤
  TASK-13 ←────────┘
       │
阶段 2（依赖 TASK-11）
  TASK-20 ←── TASK-11
  TASK-21 ←── TASK-11
  TASK-22 ←── TASK-11
  TASK-23 ←── TASK-11
       │
阶段 3（依赖 TASK-10、TASK-11）
  TASK-30 ←── TASK-11
  TASK-31 ←── TASK-10 + TASK-11
  TASK-32 ←── TASK-20
  TASK-33 ←── TASK-11
       │
阶段 4（依赖阶段 2、3）
  TASK-40 ←── TASK-31
  TASK-41 ←── TASK-23
  TASK-42 ←── TASK-13
  TASK-43 ←── TASK-00
       │
阶段 5（依赖全部前序）
  TASK-50 ←── 全部前序
  TASK-51 ←── 全部前序
  TASK-52 ←── TASK-50
  TASK-53 ←── TASK-50 + TASK-51 + TASK-52
```

---

## 多 Agent 协同建议

### 适合并行的任务组

| 并行组 | 包含任务 | Agent 数 |
|--------|----------|----------|
| **Group A（配置层）** | TASK-10、TASK-12、TASK-13 | 3 个 Agent |
| **Group B（结构层）** | TASK-20、TASK-21、TASK-22、TASK-23 | 4 个 Agent |
| **Group C（风格层）** | TASK-30、TASK-33 | 2 个 Agent |
| **Group D（文档层）** | TASK-40、TASK-42、TASK-43 | 3 个 Agent |

### 注意
- **TASK-11**（路径别名替换）会修改大量文件，建议单独执行，完成后其他 Agent 再基于新路径工作。
- **TASK-31**（统一代码风格）涉及全项目格式化，建议在所有结构性改造完成后执行一次。
- 阶段 5 的验证任务建议由同一个 Agent 串行执行，确保验证的连贯性。

---

## 约束重申

1. **不得破坏现有功能和外观** — 仅优化结构和风格。
2. **不引入外部新依赖**，除非本计划中明确列出（如 ESLint、unplugin 系列）。
3. **每个任务完成后**，Agent 应执行 `npm run dev` 验证项目可正常启动。
4. 若任何改动可能导致功能破坏，**立即停止并报告**。
